@{
    //int id = (int)ViewBag.Id;
    int id = (int)TempData["Id"];
    string str = "";
    Random rnd = new Random();
    //for (int i = 0; i < id; i++)
    //{
    //    str += $"<li>{rnd.Next(10, 90).ToString()}</li>";
    //}
}
@{ 
    Layout = "";
}
@*<div>
    <ul>
        @Html.Raw(str)
    </ul>
    
</div>*@

<div>
    <ul>
        @for (int i = 0; i < id; i++)
        {
            <li>@rnd.Next(10, 90).ToString()</li>
        }
    </ul>
</div>